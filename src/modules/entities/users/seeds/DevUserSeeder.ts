import { Seeder } from 'typeorm-seeding';

export default class DevUserSeeder implements Seeder {
	public async run(): Promise<void> {
		// 	console.log('test')
		// 	const user = await User.create({
		// 		email: 's.stiels@t-online.de',
		// 		username: '5v3n',
		// 		password: await hashPassword('XXXXXX')
		// 	}).save()
		// 	console.log(user)
	}
}
